{
  "name": "pollen-count-poller",
  "version": "1.0.0",
  "description": "A little Node.js script that parses the website of Atlanta Allergy & Asthma to get today's pollen count and pushes it to a Slack channel via incoming webhook.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ilho Song <ilho.p.song@gmail.com> (https://isong.dev)",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "node-fetch": "^2.6.0"
  }
}
